sandbox

上位ディレクトリtemplate以下にあるファイルをこのディレクトリに
コピーしてコードを書き換えてください。

たとえばtemplate/ex1にあるファイルをコピーするには

    % cd sandbox
    % cp -r ../template/ex1 .

とするとex1ディレクトリごとコピーすることができます。
行うべきことは各ディレクトリにあるREADME.mdに書いてあります。

註
mdとはmark downの意味(markupの逆の意味でたんにしゃれ)。
githubでみるとWebブラウザでみるとそれなりに成形してくれます。
